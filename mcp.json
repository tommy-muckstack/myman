{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "myman-brain": {
      "type": "stdio",
      "command": "node",
      "args": ["${PLUGIN_ROOT}/integrations/brain/server.mjs"],
      "cwd": "${PLUGIN_ROOT}"
    }
  }
}
