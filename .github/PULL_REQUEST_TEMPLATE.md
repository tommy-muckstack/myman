## What changed

## How I verified it
<!-- Both builds pass? UI changes: screenshot/recording of the running app. -->
- [ ] `swift build` passes
- [ ] `swift build -c release --arch arm64 --arch x86_64` passes
- [ ] Followed the house rules in CONTRIBUTING.md (design tokens, .clickable, panel doctrine)
