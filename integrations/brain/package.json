{
  "name": "myman-brain",
  "version": "0.11.0",
  "private": true,
  "type": "module",
  "description": "Local MyMan retrieval and explicit app actions for agents",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "start": "node server.mjs",
    "bundle": "node build-bundle.mjs",
    "check-bundle": "node build-bundle.mjs --check"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "2.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@modelcontextprotocol/client": "2.0.0",
    "ajv": "^8.17.1",
    "esbuild": "0.25.10"
  }
}
